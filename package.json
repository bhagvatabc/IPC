{
  "name": "python-shell",
  "version": "0.3.0",
  "description": "Run Python scripts from Node.js with inter-process communication through stdio",
  "keywords": [
    "python"
  ],
  "scripts": {
    "test": "mocha -R spec"
  },
  "dependencies": {},
  "devDependencies": {
    "should": "^6.0.0",
    "mocha": "^2.2.5"
  },
  "engines": {
    "node": ">=0.10"
  }
}

